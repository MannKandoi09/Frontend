// Write a program to iterate over an array of names and greet each one using forEach().

let names = ["Mann", "Meet", "Manthan"];
names.forEach(function(name){
    console.log("Hello," + name);
});